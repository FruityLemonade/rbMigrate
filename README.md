# rbMigrate
Tool to update file locations in the Alpha Theta Rekordbox database when moving files to a new location such as an external drive or new computer. A lot faster than using Rekordbox's auto-relocate feature.
